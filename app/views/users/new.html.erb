<% provide(:title, " | Sign Up") %>
<%= provide(:button_name,"Create Me") %>
<div class="new bg-img full-screen">
	<% flash.each do |key,value| %>
      <div class="flash"><%= value %></div>
    <% end %>
	<h1>Sign up </h1>
	<%= render "layouts/partials/new_edit_page" %>
</div>
